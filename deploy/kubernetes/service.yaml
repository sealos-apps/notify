apiVersion: v1
kind: Service
metadata:
  name: sealos-notify
  namespace: ns-admin
  labels:
    app: sealos-notify
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: sealos-notify
